<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             
             xmlns:mvvm="clr-namespace:Hawk.Core.Utils.MVVM;assembly=Hawk.Core"
             xmlns:themes="clr-namespace:Hawk.Core.Themes;assembly=Hawk.Core"
             xmlns:local="clr-namespace:Hawk.ETL.Controls"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             x:Class="Hawk.ETL.Controls.ETLSmartView"
             mc:Ignorable="d" 
             d:DesignHeight="553.5" d:DesignWidth="799.5">
    <UserControl.Resources>
        <local:GroupColorConverter x:Key="GroupColorConverter"/>
        <mvvm:BindingProxy x:Key="BindingProxy" Data="{Binding}"/>
        <themes:MetroConverter x:Key="MetroConverter"/>
        <Style  x:Key="CommandMenuItem" TargetType="{x:Type MenuItem}">
            <Setter Property="Header" Value="{Binding Text}"/>
            <Setter Property="Command" Value="{Binding}"/>
            <Setter Property="CommandParameter" Value="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}, Mode=FindAncestor}}"/>
        </Style>
		  



        <ItemsPanelTemplate x:Key="ItemsPanelTemplate1">
            <WrapPanel/>
        </ItemsPanelTemplate>

        <DataTemplate x:Key="DataTemplateList">
            <Grid HorizontalAlignment="Stretch" Height="65.334" Width="100" d:DesignWidth="210.332" d:DesignHeight="136.471">
                <Label Content="{Binding Name}" Margin="0" Height="Auto" VerticalAlignment="Bottom" FontSize="12" d:LayoutOverrides="GridBoX" HorizontalAlignment="Center" Width="Auto"/>
                <Rectangle Stroke="Black"  ToolTip="{Binding Description}" RadiusX="13" RadiusY="3" StrokeThickness="0" Width="45" Height="45" HorizontalAlignment="Center" VerticalAlignment="Top" MouseLeftButtonUp="Rectangle_MouseLeftButtonUp">
                    <Rectangle.Fill>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.3,0">
                            <GradientStop Color="#FF3B3B3B" Offset="0.418"/>
                            <GradientStop Color="#FF818181" Offset="0.125"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
            </Grid>
            <DataTemplate.Triggers>
                <EventTrigger RoutedEvent="Mouse.MouseEnter"/>
                <EventTrigger RoutedEvent="Mouse.MouseLeave"/>
            </DataTemplate.Triggers>
        </DataTemplate>
        <DataTemplate x:Key="DataTemplate1">
            <StackPanel Height="Auto" Width="150" d:DesignWidth="42.438" d:DesignHeight="251.312" >
                <TextBox Text="{Binding Name, Mode=TwoWay}" Background="{Binding GroupType, Converter={StaticResource GroupColorConverter}}"  ToolTip="{Binding GroupType}"  Margin="10,0" VerticalAlignment="Stretch" FontSize="14.667" FontWeight="Bold" HorizontalAlignment="Stretch" TextAlignment="Center"/>
                <ListBox AllowDrop="True" ItemTemplate="{DynamicResource DataTemplate2}" DataContext="{Binding}" ItemsSource="{Binding Value}" Drop="ListBox_Drop" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" MinHeight="200" VerticalContentAlignment="Stretch" Margin="1,0" Height="249.312" >
                    <ListBox.Resources>
                        <ContextMenu x:Key="resourceContextMenu" DataContext="{Binding Data.Commands, Source={StaticResource BindingProxy}}">
                            <MenuItem   DataContext="{Binding [0]}"  Style="{StaticResource CommandMenuItem}"/>
                            <MenuItem   DataContext="{Binding [1]}"  Style="{StaticResource CommandMenuItem}"/>
                        </ContextMenu>
                        <Style  TargetType="{x:Type ListBoxItem}">

                            <Setter Property="ContextMenu" Value="{StaticResource resourceContextMenu}" />

                        </Style>

                    </ListBox.Resources>
                    <ListBox.ContextMenu>
                        <ContextMenu  DataContext="{Binding Data.Commands, Source={StaticResource BindingProxy}}">

                            <MenuItem Style="{StaticResource CommandMenuItem}" DataContext="{Binding [2]}"/>

                        </ContextMenu>
                    </ListBox.ContextMenu>

                </ListBox>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="DataTemplate2">
            <StackPanel Height="30" Width="140" d:DesignWidth="126.167" d:DesignHeight="39.987" Orientation="Horizontal">
                <TextBlock Margin="2,0,2,0"       TextWrapping="Wrap" Text="{Binding ETLIndex}"  FontSize="14.667" Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <TextBlock Margin="2,0,8,0"  mvvm:ListBindingDoubleClick.Property="{Binding Data.Commands[0], Source={StaticResource BindingProxy}}"     TextWrapping="Wrap" Text="{Binding TypeName}"  FontSize="14.667" Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <CheckBox Content="" IsChecked="{Binding Enabled}"  VerticalAlignment="Center" HorizontalAlignment="Center" ToolTip="是否激活"/>
            </StackPanel>
        </DataTemplate>
        <SolidColorBrush x:Key="{x:Static DataGrid.FocusBorderBrushKey}" Color="#FF000000"/>
        <Style  TargetType="{x:Type xctk:MultiLineTextEditor}">
        	<Setter Property="Background" Value="{DynamicResource NormalBrush}"/>
        	<Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        	<Setter Property="Template" Value="{DynamicResource MultiLineTextEditorControlTemplate1}"/>
        	<Setter Property="BorderThickness" Value="0"/>
        	
        </Style>
        <ControlTemplate x:Key="MultiLineTextEditorControlTemplate1" TargetType="{x:Type xctk:MultiLineTextEditor}">
        	<Grid x:Name="Root">
        		<ToggleButton x:Name="PART_DropDownButton" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" Grid.Column="1" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" IsTabStop="True" IsChecked="{Binding IsOpen, RelativeSource={RelativeSource TemplatedParent}}" MinHeight="22" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}">
        			<ToggleButton.IsHitTestVisible>
        				<Binding Path="IsOpen" RelativeSource="{RelativeSource TemplatedParent}">
        					<Binding.Converter>
        						<xctk:InverseBoolConverter/>
        					</Binding.Converter>
        				</Binding>
        			</ToggleButton.IsHitTestVisible>
        			<ToggleButton.Style>
        				<Style TargetType="{x:Type ToggleButton}">
        					<Setter Property="Template">
        						<Setter.Value>
        							<ControlTemplate TargetType="{x:Type ToggleButton}">
        								<Grid SnapsToDevicePixels="True">
        									<Grid>
        										<Grid.ColumnDefinitions>
        											<ColumnDefinition Width="*"/>
        											<ColumnDefinition Width="Auto"/>
        										</Grid.ColumnDefinitions>
        										<Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
        											<ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
        										</Border>
        										<xctk:ButtonChrome x:Name="ToggleButtonChrome" Grid.Column="1" CornerRadius="0" RenderMouseOver="{TemplateBinding IsMouseOver}" RenderPressed="{TemplateBinding IsPressed}" RenderChecked="{Binding IsOpen, RelativeSource={RelativeSource FindAncestor, AncestorLevel=1, AncestorType={x:Type xctk:MultiLineTextEditor}}}" RenderEnabled="{Binding IsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorLevel=1, AncestorType={x:Type xctk:MultiLineTextEditor}}}" Visibility="Collapsed">
        											
        										</xctk:ButtonChrome>
        									</Grid>
        								</Grid>
        								<ControlTemplate.Triggers>
        									<Trigger Property="IsEnabled" Value="False">
        										<Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
        									</Trigger>
        								</ControlTemplate.Triggers>
        							</ControlTemplate>
        						</Setter.Value>
        					</Setter>
        				</Style>
        			</ToggleButton.Style>
        		</ToggleButton>
        		<Popup AllowsTransparency="True" Focusable="False" Height="{TemplateBinding DropDownHeight}" IsOpen="{Binding IsChecked, ElementName=PART_DropDownButton}" PopupAnimation="Slide" Placement="Bottom" SnapsToDevicePixels="True" StaysOpen="False" Width="{TemplateBinding DropDownWidth}">
        			<Border BorderBrush="#FFABADB3" BorderThickness="1" Background="White">
        				<Grid>
        					<TextBox x:Name="PART_TextBox" AcceptsReturn="True" HorizontalScrollBarVisibility="Auto" IsReadOnly="{TemplateBinding IsReadOnly}" SpellCheck.IsEnabled="{TemplateBinding IsSpellCheckEnabled}" Margin="3" Padding="{TemplateBinding Padding}" TextAlignment="{TemplateBinding TextAlignment}" Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" VerticalScrollBarVisibility="Auto" TextWrapping="WrapWithOverflow" Background="{TemplateBinding Background}"/>
        					<Thumb x:Name="PART_ResizeThumb" Cursor="SizeNWSE" HorizontalAlignment="Right" VerticalAlignment="Bottom">
        						<Thumb.Template>
        							<ControlTemplate TargetType="{x:Type Thumb}">
        								<Grid Background="Transparent">
        									<Path Data="M0.5,6.5L6.5,0.5 M6.5,3.5L3.5,6.5" Stroke="Black" StrokeThickness="1"/>
        								</Grid>
        							</ControlTemplate>
        						</Thumb.Template>
        					</Thumb>
        				</Grid>
        			</Border>
        		</Popup>
        	</Grid>
        </ControlTemplate>
      



    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.316*"/>
            <ColumnDefinition Width="0.684*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.264*"/>
            <RowDefinition Height="0.736*"/>
        </Grid.RowDefinitions>
     <Grid Grid.Column="1" Grid.RowSpan="2">
     	<Grid.RowDefinitions>
     		<RowDefinition Height="*"/>
     		<RowDefinition Height="25"/>
     				<RowDefinition Height="45"/>   	
		</Grid.RowDefinitions>
     	<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2">
     		<Button Style="{DynamicResource middleButton}" DataContext="{Binding Commands5[4]}" Width="35" Height="35" Margin="0,5,10,5" />
			<Button Style="{DynamicResource middleButton}" DataContext="{Binding Commands5[5]}" Width="35" Height="35" Margin="0,5,10,5" />
     		<Button Style="{DynamicResource middleButton}" DataContext="{Binding Commands5[1]}" Width="35" Height="35" />
     		<CheckBox Content="调试详情" IsChecked="{Binding DisplayDetail}" VerticalAlignment="Center"  />
     	</StackPanel>
     	<Grid Grid.Row="1">
     		<Border BorderBrush="Black" BorderThickness="1" Background="#FF2D2D2D"/>
     		<StackPanel HorizontalAlignment="Left" Margin="2.25,0,0,0" Orientation="Horizontal" d:LayoutOverrides="Height">
     			<TextBlock TextWrapping="Wrap" Text="模块:" d:LayoutOverrides="Width" Margin="5,0,10,0" VerticalAlignment="Center"/>
     			<TextBox Width="50" TextWrapping="Wrap" Text="{Binding ETLMount}" VerticalAlignment="Center" FontSize="16"/>
     			<TextBlock TextWrapping="Wrap" Text="/" d:LayoutOverrides="Width" VerticalAlignment="Center" FontSize="16"/>
     			<TextBlock TextWrapping="Wrap" Text="{Binding AllETLMount}" d:LayoutOverrides="Width" VerticalAlignment="Center" FontSize="16"/>
     		</StackPanel>
     		<ScrollBar SmallChange="1" Value="{Binding ETLMount}" Maximum="{Binding AllETLMount}" Orientation="Horizontal" Margin="131.334,4,0,4" Background="#FF292929" BorderBrush="White"/>
     	</Grid>
     	<StackPanel HorizontalAlignment="Center" Grid.Row="2" Orientation="Horizontal">
     		<Button Style="{DynamicResource middleButton}" DataContext="{Binding Commands5[2]}" Width="35" Height="35" Margin="15,0,0,0" />
     		<Button Style="{DynamicResource middleButton}" DataContext="{Binding Commands5[0]}" Width="35" Height="35" Margin="15,0,0,0" />
     		<Button Style="{DynamicResource middleButton}" DataContext="{Binding Commands5[3]}" Width="35" Height="35" Margin="15,0,0,0" />
     	</StackPanel>
     	<ScrollViewer  x:FieldModifier="public" x:Name="ScrollViewer" Margin="8" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled">
      
     		<Grid>
     			<Grid.RowDefinitions>
     				<RowDefinition Height="0.256*"/>
     				<RowDefinition Height="0.69*"/>
     				
     			</Grid.RowDefinitions>
			
     			<DataGrid x:FieldModifier="public"  ItemsSource="{Binding Documents}" Background="{DynamicResource NormalBrush}" RowBackground="{DynamicResource NormalBrush}" Foreground="{DynamicResource TextBrush}" x:Name="DataList" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="1" Margin="8" CellStyle="{DynamicResource DataGridCellStyle1}" AutoGenerateColumns="False">
     				<DataGrid.ColumnHeaderStyle>  
                    <Style TargetType="DataGridColumnHeader">  
                        <Setter Property="HorizontalContentAlignment" Value="Center">  
                        </Setter>  
                        <Setter Property="Background" Value="{DynamicResource NormalBrush}"></Setter>  
                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"></Setter>  
                        <Setter Property="BorderThickness" Value="1" />  
                        <Setter Property="BorderBrush" Value="#17acae" />  
                    </Style>  
                </DataGrid.ColumnHeaderStyle>   			
				</DataGrid>
     			<ListBox  x:FieldModifier="public" x:Name="ToolList" Margin="8" ItemsSource="{Binding Dict}"    ItemsPanel="{DynamicResource ItemsPanelTemplate1}" ItemTemplate="{DynamicResource DataTemplate1}" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VerticalContentAlignment="Stretch"/>
     			<GridSplitter HorizontalAlignment="Stretch" Height="5" VerticalAlignment="Bottom" ResizeDirection="Rows" Margin="0,0,0,-2.333" d:LayoutOverrides="VerticalAlignment, GridBox"/>
     		</Grid>
     	</ScrollViewer>
</Grid>
        <ListBox x:Name="ETLToolList"   x:FieldModifier="public"  Margin="20,44.667,20,20"  ItemsSource="{Binding ETLToolsView}"    ItemTemplate="{DynamicResource DataTemplateList}"     ItemsPanel="{DynamicResource ItemsPanelTemplate1}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.RowSpan="2" >
            <ListBox.GroupStyle>
                <GroupStyle ContainerStyle="{StaticResource GroupContainerStyle}">
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Black" BorderThickness="1" Height="30">
                                <TextBlock Text="{Binding Name}" FontSize="30" FontWeight="Bold" />
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>
        </ListBox>
        <TextBox x:Name="SearchBox" Margin="91.098,16.333,20,0" TextWrapping="Wrap"  VerticalAlignment="Top" Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock HorizontalAlignment="Left" Margin="20,16.333,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="13.742" Width="53.149"><Run Language="zh-cn" Text="快速检索:"/></TextBlock>
        <GridSplitter Margin="0,0,-2.438,0" Width="5" Grid.RowSpan="2"/>

    </Grid>
</UserControl>